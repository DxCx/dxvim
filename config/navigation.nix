_: {
  # TODO: Key bindings. =)

  # Telescope configuration (Between buffers)
  telescope.enable = true;

  # Inside buffer
  utility = {
    motion = {
      hop.enable = true;
      leap.enable = true;
      precognition.enable = false;
    };
  };
}
